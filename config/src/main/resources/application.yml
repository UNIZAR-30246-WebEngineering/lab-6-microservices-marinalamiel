server:
  port: 8762

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marinalamiel/lab6-microservices-config-repo.git

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/